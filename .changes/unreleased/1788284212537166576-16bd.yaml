kind: 'Fixed'
body: 'made the monitor retry a switch that an earlier tick could only record. A rotation deferred because a `claude` session was running was never written again, so it landed only if the shell wrapper called `ensure`, and never at all without it'
time: '2026-09-01T17:36:52.537066438Z'
