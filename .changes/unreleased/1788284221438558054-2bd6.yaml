kind: 'Changed'
body: 'stopped baking `--threshold` into the detached daemon''s command line unless it was named explicitly, so the daemon follows the stored threshold instead of treating its own startup value as an override'
time: '2026-09-01T17:37:01.438459098Z'
